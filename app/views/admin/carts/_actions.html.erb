<ul>
  <% if Admin::CartsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_carts_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_cart_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::CartsController.sortable? and Cart.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Carts"),
                 admin_carts_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Carts"),
                 admin_carts_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>